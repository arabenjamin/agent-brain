name: default
pattern: ""
priority: 9999
description: "Default fallback chain: search context, reason, self-reflect, distill into semantic knowledge."
steps:
  - tool_name: search_notes
    arguments:
      query: "{{goal}}"
      limit: 10
    priority: 1
    max_attempts: 3
    provider_hint: ollama

  - tool_name: reason
    arguments:
      question: "{{goal}}\n\nThe CONTEXT contains related notes retrieved from the knowledge graph. Use them as your primary evidence; say so explicitly if they are insufficient."
      context: "{{_prev}}"
      store_inference: true
    priority: 1
    max_attempts: 3
    provider_hint: ollama

  - tool_name: reflect_on_work
    arguments:
      goal: "{{goal}}"
      current_state: "{{_prev}}"
      task_id: "{{task_id}}"
    priority: 1
    max_attempts: 3
    provider_hint: ollama

  - tool_name: synthesize_knowledge
    arguments:
      topic: "{{goal}}"
      limit: 10
    priority: 1
    max_attempts: 3
    provider_hint: ollama

  - tool_name: update_task
    arguments:
      task_id: "{{task_id}}"
      status: completed
    priority: 1
    max_attempts: 3
    provider_hint: ollama

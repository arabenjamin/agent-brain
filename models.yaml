# models.yaml — Model catalog and context configuration
# This file is the single source of truth for model definitions.
# Loaded into DuckDB at startup for fast runtime queries.
# Add a model here and restart (or call reload_models) to make it available.

defaults:
  temperature: 0.7
  max_tokens: 4096
  timeout_secs: 120

models:
  # ── Ollama Local (http://localhost:11434) ─────────────────────────────────────
  qwen3.5:4b:
    provider: ollama
    model: "qwen3.5:4b"
    context_window: 32768
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    capabilities:
      - reasoning
      - code
    system_prompt: |
      You are agent-brain, an autonomous AI agent backed by a persistent
      Neo4j knowledge graph. You can search notes, manage tasks, reason
      over stored knowledge, and use many other tools. Think step-by-step
      before acting and use available tools for grounded answers.

  nemotron-3-nano:4b:
    provider: ollama
    model: "nemotron-3-nano:4b"
    context_window: 128000
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    capabilities:
      - reasoning
      - tools
    system_prompt: |
      You are agent-brain, an autonomous AI agent backed by a persistent
      Neo4j knowledge graph. You can search notes, manage tasks, reason
      over stored knowledge, and use many other tools. Think step-by-step
      before acting and use available tools for grounded answers.


  granite4:latest:
    provider: ollama
    model: "granite4:latest"
    context_window: 128000
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    capabilities:
      - reasoning
      - code
    system_prompt: |
      You are agent-brain, an autonomous AI agent backed by a persistent
      Neo4j knowledge graph. You can search notes, manage tasks, reason
      over stored knowledge, and use many other tools. Think step-by-step
      before acting and use available tools for grounded answers.

  gemma4:latest:
    provider: ollama
    model: "gemma4:latest"
    context_window: 128000
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    capabilities:
      - reasoning
      - code
      - vision
    system_prompt: |
      You are agent-brain, an autonomous AI agent backed by a persistent
      Neo4j knowledge graph. You can search notes, manage tasks, reason
      over stored knowledge, and use many other tools. Think step-by-step
      before acting and use available tools for grounded answers.

  # Tool-integrated computation specialist. Paired with the `execute_code` tool:
  # a step that declares `required_capabilities: ["computation"]` routes here and
  # is expected to *emit Python and run it*, not narrate arithmetic in prose.
  #
  # `computation` is deliberately held by this model ALONE. `select_models`
  # orders cost ASC then context_window DESC, and every local/ollama-cloud model
  # is $0 — so the moment a second $0 model claims `computation`, the one with
  # the largest context window wins and a 1M-context cloud model silently takes
  # every computation step. The capability is a routing selector, not a
  # description of aptitude; widen it only if you also want that reordering.
  #
  # VRAM: 4.68 GB (Q4) on an 8 GB card that already holds gemma4 + Whisper, so
  # this model swaps rather than co-resides. That is the accepted cost of a
  # correct answer on checkable work; keep computation steps rare and batched.
  qwen2.5-coder:7b:
    provider: ollama
    model: "qwen2.5-coder:7b"
    context_window: 32768
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    capabilities:
      - reasoning
      - code
      - computation
    temperature: 0.1
    system_prompt: |
      You are agent-brain's computation engine. You solve quantitative problems
      by WRITING AND RUNNING CODE, never by doing arithmetic in prose.

      For any calculation, projection, unit conversion, or data aggregation:
      call `execute_code` with a Python program that computes the result and
      prints it. Read the printed output and report that number. If the code
      errors, fix it and run again. Never state a figure you have not computed.

      State assumptions as named variables at the top of the program so the
      inputs behind a result are visible and checkable.

  # ── Ollama Cloud (https://ollama.com) ─────────────────────────────────────────
  # Free-tier models: small/open-weight, no subscription required.
  # Models that return 403 "requires a subscription" (kimi-k2.5, minimax-m2.7,
  # gemma4:31b) are excluded — add them back if you upgrade your Ollama plan.
  qwen3.5:cloud:
    provider: ollama-cloud
    model: "qwen3.5:cloud"
    context_window: 1000000
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    capabilities:
      - reasoning
      - code
    system_prompt: |
      You are agent-brain, an autonomous AI agent backed by a persistent
      Neo4j knowledge graph. You can search notes, manage tasks, reason
      over stored knowledge, and use many other tools. Think step-by-step
      before acting and use available tools for grounded answers.

 
  gemma4:31b-cloud:
    provider: ollama-cloud
    model: "gemma4:31b-cloud"
    context_window: 1000000
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    capabilities:
      - reasoning
      - code
      - vision
    system_prompt: |
      You are agent-brain, an autonomous AI agent backed by a persistent
      Neo4j knowledge graph. You can search notes, manage tasks, reason
      over stored knowledge, and use many other tools. Think step-by-step
      before acting and use available tools for grounded answers.



  ministral-3:cloud:
    provider: ollama-cloud
    model: "ministral-3:cloud"
    context_window: 131072
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    capabilities:
      - reasoning
      - code
      - fast
    system_prompt: |
      You are agent-brain, an autonomous AI agent backed by a persistent
      Neo4j knowledge graph. You can search notes, manage tasks, reason
      over stored knowledge, and use many other tools. Think step-by-step
      before acting and use available tools for grounded answers.

  deepseek-v4-flash:cloud:
    provider: ollama-cloud
    model: "deepseek-v4-flash:cloud"
    context_window: 131072
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    capabilities:
      - reasoning
      - code
      - fast
    system_prompt: |
      You are agent-brain, an autonomous AI agent backed by a persistent
      Neo4j knowledge graph. You can search notes, manage tasks, reason
      over stored knowledge, and use many other tools. Think step-by-step
      before acting and use available tools for grounded answers.

  nemotron-3-nano:cloud:
    provider: ollama-cloud
    model: "nemotron-3-nano:cloud"
    context_window: 131072
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    capabilities:
      - reasoning
      - code
      - fast
    system_prompt: |
      You are agent-brain, an autonomous AI agent backed by a persistent
      Neo4j knowledge graph. You can search notes, manage tasks, reason
      over stored knowledge, and use many other tools. Think step-by-step
      before acting and use available tools for grounded answers.


  # ── Anthropic (https://www.anthropic.com) ─────────────────────────────────
  claude-sonnet-4-6:
    provider: anthropic
    model: "claude-sonnet-4-6"
    context_window: 200000
    cost_per_1k_input: 0.003
    cost_per_1k_output: 0.015
    capabilities:
      - reasoning
      - code
      - vision
      - fast
    temperature: 0.5
    system_prompt: |
      You are agent-brain running on Claude Sonnet. Prioritize efficiency
      and tool use. Be concise.

  claude-haiku-4-5:
    provider: anthropic
    model: "claude-haiku-4-5-20251001"
    context_window: 200000
    cost_per_1k_input: 0.0008
    cost_per_1k_output: 0.004
    capabilities:
      - reasoning
      - code
      - fast
    system_prompt: |
      You are agent-brain. Use tools efficiently and be concise.

  # ── Gemini (https://ai.google.dev/gemini) ─────────────────────────────────
  gemini-2.5-flash-lite:
    provider: gemini
    model: "gemini-2.5-flash-lite"
    context_window: 1048576
    cost_per_1k_input: 0.0001
    cost_per_1k_output: 0.0004
    capabilities:
      - reasoning
      - code
      - fast
    system_prompt: |
      You are agent-brain, an autonomous AI agent backed by a persistent
      Neo4j knowledge graph. You can search notes, manage tasks, reason
      over stored knowledge, and use many other tools. Think step-by-step
      before acting and use available tools for grounded answers.

  gemini-3.1-flash-lite-preview:
    provider: gemini
    model: "gemini-3.1-flash-lite-preview"
    context_window: 1048576
    cost_per_1k_input: 0.0001
    cost_per_1k_output: 0.0004
    capabilities:
      - reasoning
      - code
      - vision
      - fast
    system_prompt: |
      You are agent-brain, an autonomous AI agent backed by a persistent
      Neo4j knowledge graph. You can search notes, manage tasks, reason
      over stored knowledge, and use many other tools. Think step-by-step
      before acting and use available tools for grounded answers.

  gemini-3.1-pro-preview-customtools:
    provider: gemini
    model: "gemini-3.1-pro-preview-customtools"
    context_window: 1048576
    cost_per_1k_input: 0.0002
    cost_per_1k_output: 0.0008
    capabilities:
      - reasoning
      - code
      - vision
      - fast
    system_prompt: |
      You are agent-brain, an autonomous AI agent backed by a persistent
      Neo4j knowledge graph. You can search notes, manage tasks, reason
      over stored knowledge, and use many other tools. Think step-by-step
      before acting and use available tools for grounded answers.

# Default system prompt used when a model doesn't define its own.
default_system_prompt: |
  You are agent-brain, an autonomous AI agent backed by a persistent
  knowledge graph. Think step-by-step and use available tools.
